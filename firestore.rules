rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the Express backend using the Admin SDK,
    // which bypasses these rules entirely. This locks out direct client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
